module.exports = {
  darkMode: 'class', // Enable dark mode using the 'class' strategy
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}', // Ensure all your JS, JSX, TS, and TSX files are included
  ],
  theme: {
    extend: {
      // Add any custom theme extensions here if needed
    },
  },
  plugins: [],
}
